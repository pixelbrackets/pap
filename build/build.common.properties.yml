settings:
  web-dir: web/
  sync-paths:
    - source: content/
      target: web/content/
      #target: ${settings.web-dir}content/
    - source: themes/
      target: themes/

live:
  user: johndoe
  host: example.com
  working-directory: /var/www/
  rsync:
    options: -razce 'ssh -p 22'
