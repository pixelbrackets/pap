settings:
  web-dir: web/
  #prepare-sync-paths:
  #  - source: src/html/build/assets/
  #    target: www/web/assets/
  sync-paths:
    - source: content/
      target: web/content/
      #target: ${settings.web-dir}content/
      #exclude: ['web/temp/', 'web/uploads/']
    - source: themes/theclassy/
      target: themes/theclassy/
  #watch-directory: src/
  #grunt:
  #  working-directory: src/html/
  #  task: live
  #composer:
  #  working-directory: www/
  #lint:
  #  php-cs-rules: '@PSR2'
  #  lint-paths:
  #    - src/typo3/

test:
  user: johndoe
  host: test.example.com
  working-directory: /var/dev/
  rsync:
    options: -razce 'ssh -p 22'

live:
  user: johndoe
  host: example.com
  working-directory: /var/www/
  rsync:
    options: -razce 'ssh -p 22'
  composer:
    phar: ${live.working-directory}composer.phar
